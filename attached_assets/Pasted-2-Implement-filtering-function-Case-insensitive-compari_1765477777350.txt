2. Implement filtering function:
- Case-insensitive comparison for source/destination.
- If trip.date exists, match exact date; else ignore date.
- Return sorted results by price (ascending) by default.

C — Frontend: results UI
1. Create or update `client/src/components/SearchResults.tsx`:
- Props: `{ results: Trip[] }`
- If results empty: show the "No buses available" message and a CTA "Search nearby" (optional).
- If results present: render the trip cards with "View Seats" button.

2. Ensure `home.tsx` renders `<SearchResults results={results} />` below the search area when `isSearched === true`.

D — Backend (optional but recommended)
1. If backend exists or will be deployed, implement `GET /api/shows`:
- File: `server/src/routes/shows.js` (or existing route)
- Query params: `from`, `to`, `date`
- Must return JSON: `{ shows: [ ... ] }`
- Implement basic DB query filtering by source/destination/date and return seats availability & price.
- If backend returns an error, frontend must fallback to client filter.

E — UX & validation
1. Disable search when fields missing:
- Button disabled attribute or visually muted.
2. Show inline error messages when the user tries to search with missing fields.
3. Keyboard: allow Enter key while focus is on any of the three inputs to trigger `onSearch()`.

F — Logging & debugging
1. Add console logs:
- console.log('SEARCH START', { from, to, date })
- console.log('SEARCH RESULTS', results.length)
2. If fallback is used, console.log('FALLBACK: local sampleTrips used').

G — Acceptance criteria
1. With sampleTrips including matching trips, clicking SEARCH shows at least one trip card and the console logs `SEARCH RESULTS X`.
2. With no matching trips, UI shows "No buses available..." message.
3. SEARCH button disabled until FROM, TO and DATE are selected.
4. Results View Seats button navigates to `/booking/:id`.

H — Files expected to change / create
- client/src/pages/home.tsx (UPDATE)
- client/src/components/SearchResults.tsx (CREATE/UPDATE)
- client/src/data/sampleTrips.ts (CREATE/UPDATE)
- server/src/routes/shows.js (OPTIONAL CREATE/UPDATE)
- client/src/utils/api.ts (CREATE/UPDATE helper for fetch + timeout)
- client/src/utils/currency.ts (ensure formatter exists)

Start now and return:
1) The exact list of changed files (paths).  
2) A short console log snippet showing `SEARCH START` and `SEARCH RESULTS`.  
3) Two screenshots: (a) results list with matching buses, (b) “No buses available” message.

If any runtime errors occur while implementing, return the full console error text immediately.
