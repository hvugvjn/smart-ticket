Implement full multilingual support for the entire NexTravel frontend using i18next + react-i18next.

REQUIREMENTS:
1. Add support for these languages:
   - English (default)
   - Hindi
   - Tamil
   - Telugu
   - Kannada

2. Create a folder:
   client/src/i18n/

3. Inside it create translation JSON files:
   - en.json
   - hi.json
   - ta.json
   - te.json
   - kn.json

4. Each JSON file must contain keys for all UI text:
   {
     "home": {
       "title": "Experience the Future of Travel",
       "subtitle": "Premium intercity bus booking with intelligent seat selection."
     },
     "search": {
       "from": "From",
       "to": "To",
       "date": "Date",
       "search": "Search"
     },
     "booking": {
       "pickup": "Pickup Point",
       "drop": "Drop Point",
       "track": "Track my bus live"
     },
     "ticket": {
       "bookingConfirmed": "Booking Confirmed!",
       "download": "Download Ticket",
       "cancel": "Cancel Booking"
     }
   }

5. Add i18n initialization file:
   client/src/i18n/index.ts
   - Import all languages
   - Setup i18next
   - Use localStorage to remember selected language

6. Wrap the root application with I18nextProvider:
   client/src/main.tsx or client/src/index.tsx

7. Add a Language Selector component:
   client/src/components/LanguageSwitcher.tsx
   - Dropdown with options: English, हिंदी, தமிழ், తెలుగు, ಕನ್ನಡ
   - On select, update i18next language + store in localStorage

8. Add LanguageSwitcher to the Navbar:
   client/src/components/Navbar.tsx
   - Display as small globe icon or simple dropdown at top-right

9. Replace all visible UI text across the site with translation keys:
   Instead of:
     <h1>Experience the Future of Travel</h1>
   Use:
     <h1>{t("home.title")}</h1>

10. Ensure multilingual changes apply to:
   - Home page
   - Search bar
   - Booking page (pickup/drop, seat selection)
   - Confirmation page
   - My Trips page
   - Admin pages
   - Buttons, labels, forms, errors

11. After implementing, test:
   - Switch language → UI updates instantly
   - Refresh page → selected language persists
   - All text replaced with translation keys, no hardcoded strings left.

Return the full list of files created/edited and confirm successful i18n integration.
